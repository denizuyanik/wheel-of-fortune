import { withDefaults } from '@wix/react-component-utils';
import WheelWidget from './wheel-widget';
import { defaultProps } from './wheel-widget.props';

export default withDefaults(WheelWidget, defaultProps);
